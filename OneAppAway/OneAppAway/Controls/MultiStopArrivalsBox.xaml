<UserControl
    x:Class="OneAppAway.MultiStopArrivalsBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OneAppAway"
    xmlns:converters="using:OneAppAway.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="450"
    d:DesignWidth="300" RequestedTheme="Dark">

    <Grid Margin="0,0,0,5" x:Name="MainGrid">
        <ScrollViewer x:DeferLoadStrategy="Lazy" Background="Transparent" Margin="5" x:Name="scrollViewer" VerticalScrollMode="Disabled" HorizontalScrollMode="Enabled" HorizontalScrollBarVisibility="Visible" Tag="0" VerticalScrollBarVisibility="Disabled">
            <StackPanel Orientation="Horizontal" x:Name="ItemsPanel" HorizontalAlignment="Left"/>
        </ScrollViewer>
        <Grid x:DeferLoadStrategy="Lazy" x:Name="SingleStopControl" Margin="5" HorizontalAlignment="Stretch"/>
    </Grid>
</UserControl>
