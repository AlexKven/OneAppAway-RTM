<local:StopArrivalsControlBase x:ConnectionId='1'
    x:Class="OneAppAway._1_1.Views.Controls.StopArrivalsSubControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OneAppAway._1_1.Views.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:conv="using:OneAppAway._1_1.Converters"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400" MinHeight="200"                                                                >

    <Grid>
        <Grid.Resources>
            <conv:BoolToVisibilityConverter x:Key="BoolVisibilityConverter"/>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <!--<Rectangle>
            <Rectangle.Fill>
                <LinearGradientBrush EndPoint="1,0" StartPoint="0.5,0">
                    <GradientStop Color="#00202020"/>
                    <GradientStop Color="#80202020" Offset="1"/>
                </LinearGradientBrush>
            </Rectangle.Fill>
        </Rectangle>
        <Rectangle Fill="#80202020" Grid.Column="1"/>
        <Rectangle Grid.Column="2">
            <Rectangle.Fill>
                <LinearGradientBrush EndPoint="0.5,0">
                    <GradientStop Color="#80202020"/>
                    <GradientStop Color="#00202020" Offset="1"/>
                </LinearGradientBrush>
            </Rectangle.Fill>
        </Rectangle>-->
        <Rectangle Grid.ColumnSpan="3" Fill="#303030" Visibility="{Binding Path=IsTopLevel, Converter={StaticResource BoolToVisibilityConverter}}"/>
        <Image Width="40" Height="40" Source="{Binding IconUri}" VerticalAlignment="Top"/>
        <Button Height="38" Background="Transparent" Template="{ThemeResource SimpleButtonTemplate}" Grid.Column="1" Margin="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <local:AutoFitTextBlock Text="{Binding StopName}" FontSize="18" Foreground="White" VerticalAlignment="Stretch"/>
        </Button>
        <StackPanel Orientation="Horizontal" Grid.Column="2" VerticalAlignment="Top">
            <Button RequestedTheme="Dark" Background="Gray" Foreground="White" Width="40" Height="40" FontFamily="Segoe UI Symbol" FontSize="16" Margin="0,1,1,0" Visibility="{Binding Path=HasChildren, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=invert}"></Button>
            <Button x:ConnectionId='3' RequestedTheme="Dark" Background="Gray" Foreground="White" Width="40" Height="40" FontFamily="Segoe MDL2 Assets" FontSize="16" Margin="0,1,1,0" x:Name="CompressButton">&#xE73F;</Button>
            <Button x:ConnectionId='4' RequestedTheme="Dark" Background="Gray" Foreground="White" Width="40" Height="40" FontFamily="Segoe MDL2 Assets" FontSize="16" Margin="0,1,1,0" x:Name="ExpandButton">&#xE740;</Button>
            <Button x:ConnectionId='5' RequestedTheme="Dark" Background="Red" Foreground="White" Width="40" Height="40" FontFamily="Segoe UI Symbol" FontSize="16" Margin="0,1,1,0" x:Name="CloseButton">❌</Button>
        </StackPanel>
        <ScrollViewer Grid.Row="1" Grid.ColumnSpan="3" VerticalScrollMode="Disabled" VerticalScrollBarVisibility="Hidden" HorizontalScrollMode="Auto" HorizontalScrollBarVisibility="Auto" Visibility="{Binding HasChildren, Converter={StaticResource BoolToVisibilityConverter}}">
            <ItemsControl Grid.Row="1" Grid.ColumnSpan="3" ItemsSource="{Binding ChildrenSource}" VerticalContentAlignment="Stretch" Margin="5" Visibility="{Binding HasChildren, Converter={StaticResource BoolToVisibilityConverter}}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <local:StopArrivalsSubControl VerticalAlignment="Stretch" Width="{Binding Width}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        <Grid Grid.Row="1" Grid.ColumnSpan="3" Visibility="{Binding HasChildren, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=invert}">
            <Grid.RowDefinitions>
                <RowDefinition Height="45"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <ProgressBar VerticalAlignment="Top" Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}" IsIndeterminate="{Binding IsBusy}"/>
            <ScrollViewer VerticalScrollMode="Disabled" VerticalScrollBarVisibility="Hidden" VerticalContentAlignment="Stretch" HorizontalContentAlignment="Left" HorizontalScrollMode="Enabled" HorizontalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding RouteNames}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Background="#303030" Template="{ThemeResource SimpleButtonTemplate}" Margin="1">
                                <Border BorderBrush="{ThemeResource SystemColorControlAccentBrush}" BorderThickness="2" MinWidth="43" Height="43">
                                    <TextBlock Margin="2, 0, 2, 0" Foreground="White" FontSize="20" Text="{Binding}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
            <local:StopArrivalsBox x:ConnectionId='2' Grid.Row="1" x:Name="ArrivalsBox" RequestedTheme="Dark" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
        </Grid>
    </Grid>
</local:StopArrivalsControlBase>

