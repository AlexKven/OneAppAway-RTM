<Page
    x:Class="OneAppAway._1_1.Views.Pages.TransitMapPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:localP="using:OneAppAway._1_1.Views.Pages"
    xmlns:localC="using:OneAppAway._1_1.Views.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:ConnectionId='1' Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" x:Name="MainGrid">
        <localC:TransitMap x:ConnectionId='2' x:Name="MainMap">

        </localC:TransitMap>
        <Border HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Grid.Row="1" IsHitTestVisible="False">
            <Border.Background>
                <LinearGradientBrush EndPoint="0,1">
                    <GradientStop Color="Transparent"/>
                    <GradientStop Color="#88333333" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <TextBlock IsHitTestVisible="False" Margin="0,20,0,0" HorizontalAlignment="Center" TextTrimming="CharacterEllipsis" Foreground="White">Public transit data powered by OneBusAway.</TextBlock>
        </Border>
        <ProgressBar x:ConnectionId='3' HorizontalAlignment="Stretch" VerticalAlignment="Top" IsHitTestVisible="False" Visibility="Collapsed" x:Name="LoadingIndicator"/>
        <CommandBar x:ConnectionId='4' x:Name="MainControlBar" RequestedTheme="Dark" Background="#303030" Grid.Row="2" BorderBrush="#303030">
            <AppBarButton x:ConnectionId='5' x:Name="CurrentLocationButton" Icon="MapPin" Label="Current Location"                                    />
            <AppBarButton x:ConnectionId='6' x:Name="ZoomInButton" Icon="ZoomIn" Label="Zoom In"                            ClickMode="Press"/>
            <AppBarButton x:ConnectionId='7' x:Name="ZoomOutButton" Icon="ZoomOut" Label="Zoom Out"                             ClickMode="Press"/>
            <AppBarButton x:ConnectionId='8' x:Name="RefreshButton" Label="Find Stops"                            >
                <AppBarButton.Icon>
                    <BitmapIcon UriSource="/Assets/Icons/RefreshStopsIcon.png"/>
                </AppBarButton.Icon>
            </AppBarButton>
        </CommandBar>
    </Grid>
</Page>

