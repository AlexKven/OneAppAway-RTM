<UserControl
    x:Class="OneAppAway._1_1.Views.Controls.RealTimeArrivalControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OneAppAway._1_1.Views.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400" Width="50">
    <Button Template="{ThemeResource SimpleButtonTemplate}" Background="Transparent" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Opacity="{Binding IsDropOffOnly, Converter={StaticResource FadeOnDropOffOnlyConverter}}">
        <Grid Height="45">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="45"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto" MinWidth="120"/>
                <ColumnDefinition Width="45"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Rectangle Opacity="1" Grid.RowSpan="2" Fill="{Binding DegreeOfConfidence, Converter={StaticResource DegreeOfConfidenceToBackgroundConverter}, ConverterParameter={StaticResource SystemColorControlAccentColor}}"/>
            <local:AutoFitTextBlock FontSize="24" Grid.RowSpan="2" Margin="5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Text="{Binding RouteName}" RequestedTheme="Dark"/>
            <local:AutoFitTextBlock FontSize="28" Grid.RowSpan="2" Grid.Column="4" Margin="5,5,15,5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Text="{Binding MinutesAway}" Foreground="{Binding IsEarly, Converter={StaticResource IsEarlyToForegroundConverter}, ConverterParameter={StaticResource SystemColorControlAccentColor}}"/>
            <TextBlock Grid.Column="3" Margin="2,0,2,0" Text="{Binding PredictedArrivalTimeText}" Foreground="{Binding IsPredicted, Converter={StaticResource IsPredictedToForegroundConverter}, ConverterParameter={StaticResource SystemColorControlAccentColor}}"/>
            <TextBlock Grid.Column="3" Grid.Row="1" Margin="2,0,2,0" Text="{Binding ScheduledArrivalTimeText}"/>
            <TextBlock Grid.Column="2" Grid.RowSpan="2" VerticalAlignment="Center" MaxHeight="45" HorizontalAlignment="Left" Margin="5,2,5,2" TextWrapping="WrapWholeWords" TextTrimming="WordEllipsis" FontSize="10" Text="{Binding RouteDestination}"/>
            <Image Source="/Assets/Icons/Alert.png" Margin="5" Grid.Column="1" Grid.RowSpan="2" VerticalAlignment="Stretch" Stretch="Uniform" Visibility="{Binding HasAlert, Converter={StaticResource BoolToVisibilityConverter}}"/>
            <Image Source="/Assets/Icons/Cancel.png" Margin="5" Grid.RowSpan="2" VerticalAlignment="Stretch" Stretch="Uniform" Visibility="{Binding IsCancelled, Converter={StaticResource BoolToVisibilityConverter}}"/>
        </Grid>
    </Button>
</UserControl>

