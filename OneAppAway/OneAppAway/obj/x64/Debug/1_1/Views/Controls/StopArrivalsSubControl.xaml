<local:StopArrivalsControlBase
    x:Class="OneAppAway._1_1.Views.Controls.StopArrivalsSubControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OneAppAway._1_1.Views.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:conv="using:OneAppAway._1_1.Converters"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400" MinWidth="150" MinHeight="200">

    <Grid>
        <Grid.Resources>
            <conv:BoolToVisibilityConverter x:Key="BoolVisibilityConverter"/>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="{Binding StopName}" Margin="4" FontSize="20" HorizontalAlignment="Center" TextTrimming="WordEllipsis" Foreground="White"/>
        <Button Grid.Column="1" RequestedTheme="Dark" Background="Gray" Foreground="White" Width="40" Height="40" FontFamily="Segoe UI Symbol" FontSize="16" Margin="1" Visibility="{Binding Path=HasChildren, Converter={StaticResource BoolVisibilityConverter}, ConverterParameter=invert}"></Button>
        <ItemsControl Grid.Row="1" Grid.ColumnSpan="2" ItemsSource="{Binding ChildrenSource}" VerticalContentAlignment="Stretch" Margin="5">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <local:StopArrivalsSubControl VerticalAlignment="Stretch"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</local:StopArrivalsControlBase>

